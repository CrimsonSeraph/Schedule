# 模块 README 模板

> 本文件是**模板**，不是任何模块的文档。新建或重写模块 `README.md` 时复制本文件，替换尖括号占位内容，并删除本引用块与所有“模板说明”注释。
>
> 约束（来自项目开发要求）：除根目录 `README.md` 外，每个模块目录必须有一份 `README.md`，且必须覆盖下列全部章节，章节标题与顺序保持一致，便于跨模块对照维护。

---

## 职责

<用 3~6 句话说明本模块“负责什么”与“明确不负责什么”。不负责的部分要写清楚，避免职责蔓延。>

## 依赖

| 依赖            | 类型   | 说明       |
| --------------- | ------ | ---------- |
| `<ScheduleXxx>` | 项目内 | <为何依赖> |
| `Qt6::<Module>` | 外部   | <为何依赖> |

- 允许依赖：<上层/下层模块>
- 禁止依赖：<列出反向依赖，例如“不得依赖 engine / ui / app”>

## 产物

- 目标名：`<ScheduleXxx>`（静态库 / QML 模块 / 可执行程序）
- 公开头文件目录：`include/`（引用方式 `#include "<xxx>/<Header>.h"`）
- <其他产物：QML 模块 URI、资源前缀、生成文件等>

## 目录结构

```text
src/<模块>/
├── CMakeLists.txt
├── README.md
├── include/<模块>/       # 公开头文件
└── src/                  # 实现文件
```

## 公开接口与关键类型

<逐个列出公共类 / 结构体 / 枚举 / 自由函数，并说明语义、线程与生命周期约束。>

| 类型     | 头文件         | 说明   |
| -------- | -------------- | ------ |
| `<Type>` | `<xxx/Type.h>` | <说明> |

## 构建与测试方式

```bash
# 配置（首次）
cmake --preset windows-msvc
# 构建
cmake --build --preset windows-msvc-debug
# 单元测试（需 -DBUILD_TESTS=ON）
ctest --preset windows-msvc
```

- 本模块测试文件位置：`<tests/...>`
- 本模块是否可在无 GUI 环境下测试：<是/否，原因>

## 与上下层交互方式

- 向下：<调用哪些下层接口>
- 向上：<向上层暴露什么，如何被组装>

## 信号连接约定

- 桥接对象与 QML 之间的信号连接**统一在 C++ 侧显式建立**（`QObject::connect`）；QML 中不写 `onClicked` / `Connections` 等按名称隐式连接的写法。
- 槽与信号命名：全小写 + 下划线，例如槽 `test_button_clicked()`、信号 `test_signal(message)`。
- <列出本模块对外信号及其触发时机、参数含义>

## 扩展点与注意事项

- 扩展点：<新增实现应继承/实现哪个接口，注册到哪里>
- 注意事项：<平台差异、边界条件、性能、线程、编码等易错点>

## 相关文档

- [根 README](../README.md)
- [阶段路线图](../docs/ROADMAP.md)
- <其他相关模块 README>
